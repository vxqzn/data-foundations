User → stores stats, calculates TDEE, sets target (cut/bulk/maintain)
Food → name, macros per 100g (born with these, they don't change)
MealEntry → a Food + grams logged → calculates actual macros for that portion
DailyLog → collection of MealEntries → running total vs target, end-of-day summary